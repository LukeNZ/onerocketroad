export class Image {
    public summary: string;
    public attribution: string;

    constructor(summary: string, attribution: string) {
        this.summary = summary;
        this.attribution = attribution;
    }
}